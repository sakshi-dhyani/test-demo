version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto17
  build:
    commands:
      - java -version
      - mvn package
artifacts:
  files:
    - '**/*'
  base-directory: 'target/farelabs-0.0.1-SNAPSHOT.jar'
